[pytest]
log_cli=true
log_level=ERROR
log_print=true
log_format = %(asctime)s %(filename)s %(funcName)s:%(lineno)d %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

addopts = -ra -l --tb=short --keep-duplicates
